apiVersion: v1
data:
  {{- (.Files.Glob "assets/**.json").AsConfig | nindent 2 }}
kind: ConfigMap
metadata:
  name: rook-ceph-dashboards
  #labels:
    #  - include "rook-ceph-dashboards.labels" . | nindent 4 
  namespace: "{{ .Release.Namespace }}"
  labels:
    grafana_dashboard: "1"
